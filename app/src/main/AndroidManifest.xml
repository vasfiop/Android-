<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light">
        <activity android:name=".MainActivity"></activity>


        <activity android:name=".ExpandableListviewActivity" />
        <activity android:name=".CheckBoxMoreActivity" />
        <activity android:name=".CheckBoxActivity" />
        <activity android:name=".ClockChronometerActivity" />
        <activity android:name=".DataTimePickerActivity" />
        <activity android:name=".DIYCtrlActivity" />
        <activity android:name=".AutoCompleteTextView" />
        <activity android:name=".TestActivity" />
        <activity android:name=".first">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".decAvtivity" />

    </application>

</manifest>